interface:
  display_name: "Codex Pro Dispatch"
  short_description: "Desktop-only dispatch to ChatGPT Pro"
  default_prompt: "Use $codex-pro-dispatch to send this bounded assignment to my dedicated ChatGPT Pro worker and verify the result."
policy:
  allow_implicit_invocation: false
