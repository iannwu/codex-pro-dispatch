# v1.2.2 release acceptance

Date: 2026-09-05. Candidate runtime: published `v1.2.2-rc.1`, commit
`ac4d1748018a13c83efea5c915a0a1d06abe9b51`. Intended tag: `v1.2.2`.
App version `26.901.41600`, build `7982`; macOS `26.6.2` (`25G83`), arm64;
local Python `3.14.6`. Native controls used: `list_threads`, `read_thread`,
`send_message_to_thread`, and `navigate_to_codex_page`, with stable parent and
worker IDs resolved before dispatch. Pro selection was user-confirmed.

**Release accepted with the clipboard-verification exception below.** The
maintainer requested deployment after the unverified check was disclosed and
confirmed that no interruption was observed. This is not an unconditional A–L
pass. Release packaging changes version metadata, documentation, and one test
assertion that could falsely match timestamp digits. The runtime and skill
protocol exercised on the candidate remain unchanged.

## Additional verified cases

- Same-worker context: the first exchange acknowledged a fixed fact; the next
  exchange returned that fact exactly. Distinct assignment IDs, one send each,
  matching native user read-back, complete receipts, and exact parent restoration.
- Live stale-message isolation: the previous native user message was rejected as
  `stale-readback`; the previous full native exchange was rejected as
  `native-reply-not-observed`. The current receipt remained byte-for-byte unchanged
  after both rejections, and the matching new response later completed normally.
- Long result: exact **36,485 ASCII characters/UTF-8 bytes** from five chunks.
  Payload lengths were **10,249 / 6,559 / 6,559 / 6,559 / 6,559** bytes; final flags
  were **0 / 0 / 0 / 0 / 1**. The intact first response exceeded the 10,000-byte
  guideline and was accepted below the native reader boundary.
- Every chunk matched its locally generated section. The combined bytes matched
  the complete fixture, with no inserted separators and a private fsynced assembly.
- Local partial-write injection: the same assembly verification script was run
  against an isolated copy of the accepted first-chunk result. An injected write
  failure after 17 bytes stopped the runner without a success checkpoint. No
  receipt changed and no assignment was prepared; the partial files were removed.
  This was a controlled local failure test, not an additional native exchange.
- Isolated checks of unasserted preflight, HTTP 403 recording/cooldown denial,
  explicit-time cooldown expiry, legacy redaction, and durable armed-state
  no-resend behavior passed. These do not claim a real app restart occurred.
- Selected-scope truncation, missing/footer-boundary cases, and wrong chunk
  expectations passed their targeted rejection tests.
- Worker config/receipts were mode `0600`; transient directory/files were
  `0700`/`0600`. All eight live exchanges completed with one send attempt each.
- Real app-restart recovery also passed. After one acknowledged native send,
  submission recording was deliberately withheld and the user quit/reopened
  ChatGPT. The on-disk receipt survived as `armed`, with submission count zero
  and `no_resend=true`. Recovery found exactly one matching native user message,
  verified the original prompt, and accepted the expected 43-byte reply by its
  pre-recorded SHA-256. The completed receipt has submission count one. There
  was no send during recovery, no remaining active assignment/cooldown, and the
  exact parent was restored before temporary files were removed.
- The restarted app log records `thread not loaded` for the requested ChatGPT
  worker in the initial Codex `thread/read` path at 20:51:30 UTC. The host's
  ChatGPT fallback returned history successfully; the parent then explicitly
  opened the exact worker and completed collect-only recovery. This is evidence
  of the real internal not-loaded path, not a claim that the outer read tool
  surfaced an unhandled error. Refetch completion was logged at 20:52:07 UTC.

## Reproducible long fixture

Use sections S=1..5. Section 1 contains N=001..125; sections 2..5 each contain
N=001..080. Each line is:

```text
RECOVERY-S{S}-{N}|ABCDEFGHIJKLMNOPQRSTUVWXYZ|abcdefghijklmnopqrstuvwxyz|0123456789|
```

Join lines within each section with LF, without leading/trailing LF. Concatenate
the five sections without additional separators. Verified SHA-256:
`8710585695d29072696e297ac25fb09a7fb545029349015496b3959b3b7de558`.

## Release-specific exception

- **B/H clipboard observation:** no clipboard API was used. Clipboard contents
  were not measured before/after; the user could not assess whether they changed.
  Clipboard preservation remains unverified. The maintainer accepted deployment
  with this limitation disclosed. This exception does not change the default
  acceptance checklist or authorize clipboard use.

## Background and GitHub checks on the tagged candidate

The user switched to another application before the background assignment.
Native send started at 21:04:48 UTC and succeeded at 21:04:49 UTC. The app log
reports `rendererWindowFocused=false` at both boundaries. Collection was deferred
until the user returned several minutes later. The user confirmed that no
interruption was observed. This observation does not verify clipboard contents.
The expected 46-byte reply matched its recorded
SHA-256, with one matching user message and one submission. The exact parent was
restored, all three private transient files were removed, and the unique test
sentinel was absent from the checkout and helper configuration/state.

A fresh GitHub write also passed on this tagged runtime and the new worker.
The worker created commit `03ca0538e0d6dc5e39fbb2080dbfc6e95b3266e3` on
`codex/v1.2.2-stable-acceptance-20260905`. The parent independently fetched the
remote objects and verified:

- sole parent `ac4d1748018a13c83efea5c915a0a1d06abe9b51`;
- exactly one added regular file, mode `100644`, at
  `docs/acceptance-probes/v1.2.2-stable-20260905.txt`;
- the requested 36 bytes, SHA-256
  `a68c54cbc9fc363c37693ad02097087cf4c51ec12326c91fef252e78ecb899c1`;
- exact commit message `test: verify v1.2.2 stable dispatch`;
- branch head at the reported commit and `main` unchanged at the candidate.

The parent performed no substitute write. The audit branch was left unmerged;
branch cleanup preserves its commit in the maintainer's verified Git bundle.
The exchange completed with one submission; the parent was restored before
private transient files were removed.

## Matrix reconciliation

| Case | Result and evidence scope |
| --- | --- |
| A | Passed native capability checks, user-confirmed Pro configuration and private modes; isolated unasserted preflight rejected preparation. |
| B | Exact one-turn responses and parent restoration passed; clipboard preservation unverified. |
| C | Real internal `thread not loaded` path and collect-only recovery passed as described above. |
| D | Two-turn memory passed in the same worker. |
| E | Real app restart after send/before recording passed with no resend; zero-send boundary covered by isolated tests. |
| F | Live stale user/history rejected without receipt mutation; marker mismatch tests passed. |
| G | New worker's remote write independently verified on the tagged runtime. |
| H | Background send and deferred collection passed with log evidence and user confirmation of no interruption; clipboard preservation unverified. |
| I | Isolated simulated unusual-activity 403, abandonment/cooldown denial and explicit-clock expiry passed. No real service 403 was induced. |
| J | Private modes, parent-first cleanup, sentinel absence and simulated write-failure cleanup passed. |
| K | Isolated legacy diagnostic redaction and corrupt-receipt checks passed. |
| L | Exact 36,485-byte assembly, 10,249-byte overshoot, rejection cases and injected partial-write stop passed. |

Computer-use access to the ChatGPT/Codex app is blocked in this environment.
The user performed the app restart and foreground check. No computer-use
workaround, clipboard access, or resend was used.

The verification contract remains `bounded_native_summary`; original source-byte
and generation-finality verification remain false.

## Packaging validation

All 94 local tests passed, along with Python compilation, shell syntax, CLI help,
skill validation, and whitespace checks. The native-summary privacy test now
checks distinctive response text instead of three digits that can legitimately
appear in timestamps or hashes. A file comparison confirmed every runtime and
skill-protocol file matches the tested candidate, allowing only version metadata.
The release PR and merged commit must also pass macOS/Linux CI before tagging.
