# v1.3.0 acceptance receipt

Status: stable release qualification.

## Candidate

- Version: `1.3.0`
- Implementation commit: `b1b5630`
- Release commit: recorded by the `v1.3.0` tag
- Scope: one resident listener with one or two configured ChatGPT workers
- Native transport: official ChatGPT/Codex desktop app only
- App: `26.915.31029` build `9771`
- Host: macOS `26.6.2`, arm64

## Automated gates

- Python test suite: 301 passed
- Node test suite: 299 passed
- Diff integrity check: passed
- Dual-worker regression: overlapping deliveries use distinct configured slots
  while one listener retains admission for the sibling request
- Recovery regression: post-arm work stays collect-only, request identity is
  preserved, and no recovery path authorizes another native send
- Listener replacement regression: fenced generations cannot resume or admit
  work after a replacement owner takes over

## Live native qualification

Maintainer-run native tests on the release implementation confirmed:

- Claude Code to Codex listener to ChatGPT completed with one send, matching
  repeated collection hashes, durable save, and acknowledgement
- the listener remained healthy and advanced to the next ordinal after a request
- a capacity-two pool admitted two overlapping requests to separate worker slots
- both requests completed with one observed submission each
- listener replacement and generation fencing recovered a deleted or unavailable
  owner without replaying completed work
- model and reasoning selection remained user-controlled in each ChatGPT worker

Automatic launch after app or laptop restart is not claimed. The supported
recovery is an explicit guarded listener restore or replacement. Exact source
bytes and generation finality remain outside the native summary contract.

## Distribution

The release is published from the reviewed pull request and tagged `v1.3.0`.
This receipt omits worker IDs, parent task IDs, session paths, request IDs,
prompt contents, and private evidence locations.
