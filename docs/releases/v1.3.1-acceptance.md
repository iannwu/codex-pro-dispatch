# v1.3.1 acceptance receipt

Status: stable patch release qualification.

## Candidate

- Version: `1.3.1`
- Runtime implementation: the reviewed PR #21 file set, replayed without content changes onto current `main`
- Scope: resident owner supervision, verified outbound delivery, and exact packet-stage execution
- Native transport: official ChatGPT/Codex desktop app only
- App: `26.915.31029` build `9771`
- Host: macOS `26.6.2`, arm64

## Automated gates

- Python test suite: 312 passed
- Node test suite: 292 passed on the exact landing branch
- GitHub CI: macOS and Ubuntu passed on the reviewed implementation before the release-only metadata update
- Diff integrity check: passed
- Resident supervision regression: owner finalization stays blocked until active work joins and cleanup completes
- Packet execution regression: private stages are bound by SHA-256, byte length, and directive before execution
- Recovery regression: post-arm work remains collect-only and no path authorizes another native send

## Live native qualification

A real Claude Code client used the installed skill against the dedicated Listener
and a configured ChatGPT worker. The qualification confirmed:

- the Listener opened and served under its original supervised execution
- one fresh request made exactly one native submission
- the worker returned the exact framed `DISPATCH-OK` result
- the assignment completed with `submission_count: 1`
- the queue receipt moved to `acknowledged`
- both configured worker slots returned to idle
- the Listener waiting marker continued heartbeating after completion
- historical collect-only evidence remained preserved and was not resent

The response used bounded native-summary verification. Exact original source
bytes and authoritative generation finality remain outside the contract.
Automatic launch after app or laptop restart is not claimed.

## Distribution

The release is published from the reviewed pull request and tagged `v1.3.1`.
This receipt omits worker IDs, parent task IDs, session paths, request IDs,
prompt contents, and private evidence locations.
