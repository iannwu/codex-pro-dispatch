# v1.3.0-rc.1 acceptance receipt

Status: local release candidate only. This is not a stable or public release.

## Candidate

- Version: `1.3.0-rc.1`
- Commit: recorded after the final candidate commit
- Scope: one resident listener, one dedicated Pro worker, sequential requests
- Native transport: official ChatGPT/Codex desktop app only

## Local gates

- Python test suite: 158 passed
- Node test suite: 143 passed
- Python compilation, shell syntax, CLI help, skill validation, and staged diff
  checks: passed
- Markdown link check and version alignment check: passed
- Listener admission regression: deterministic wrapped-prompt rejection occurs
  before queue or native receive state is created
- Queue integrity regression: duplicate queue keys fail closed without mutation

## Live gates

The following gates require the parent desktop task and are not claimed here:

- Current parent and configured worker identity from native context
- Exact-ID native send, outbound read-back, paired history validation, and exact
  parent restoration
- Actual Claude client registration and a bounded live native qualification
- Connector-specific repository read or write acceptance
- App restart, locked-screen, and long-running resident availability behavior

No historical native request was replayed. No browser, private endpoint, state
reset, permission bypass, public tag, push, merge, or release action was used.

## CI and distribution

Remote CI is pending because this candidate was not pushed. No public tag or
release was created. The existing local installation remains the prior local
candidate snapshot until an explicit installation decision is made.

This receipt intentionally omits worker IDs, parent task IDs, session paths,
request IDs, prompt contents, and private evidence locations.
