# v1.1.0 native acceptance receipt

- Date: 2026-08-26
- Intended tag: `v1.1.0`
- Tested candidate commit: `92928c8c7260b73b93553be6875d9d3dd45c38f6`
- Candidate tree: `9bf7bf21764a3c605a30420c84d766f591302aeb`
- App: ChatGPT/Codex desktop `26.820.60940` (`7119`)
- Codex CLI: `0.149.0`
- Host: macOS 26.6.2 (`25G83`), arm64
- Worker model evidence: user-confirmed Pro in one dedicated Chat conversation
- Result: pass

The final release-status commit changes documentation only. Runtime, skill, plugin, installer, and test files are identical to the tested candidate above.

## Native capabilities

The parent used stable task and conversation IDs with native task listing, exact-ID submission, exact user-message read-back, completed-response collection, and exact-ID navigation/restoration. No browser, Accessibility, AppleScript, CDP, or clipboard mechanism was used to transport an assignment.

## Matrix

| Gate | Result | Redacted evidence |
| --- | --- | --- |
| A. Setup | Pass | Unasserted health and prepare failed closed; asserted health passed; worker config and state were private. |
| B. One-turn roundtrip | Pass | One armed native send returned the exact `CODEX_TO_CHAT_PRO_OK_7319` payload with the expected result marker. |
| C. Thread-not-loaded recovery | Pass | Delayed native history visibility was handled collect-only by exact worker ID; no resend occurred. |
| D. Same-worker continuation | Pass | Two unique assignments in the same worker returned `STORED_7319` and then `ORCHID-CLOCK-7319`. |
| E. At-most-once uncertainty | Pass | Live delayed-read recovery and deterministic interruption tests preserved `no_resend: true`. |
| F. Marker isolation | Pass | Stale greeting and earlier marked responses coexisted; only the current assignment marker completed. Mismatch tests passed. |
| G. GitHub worker proof | Pass | Pro created disposable branch `probe/v1.1.0-92928c8-pro-7319`; parent verified commit `0d9b16bf03a993a81da92ab8f53e88d048323fe6`, its exact base, message, sole file, bytes, and unchanged protected `main`. No PR or merge was created. |
| H. Foreground behavior | Pass with documented limitation | The multi-minute GitHub dispatch completed without clipboard transport; collection used the documented exact-task navigation and restoration behavior. |
| I. HTTP 403 cooldown | Pass | CLI and core tests preserved collect-only state, request metadata, and the fixed 1,800-second cooldown after authorized abandonment. |
| J. Private transient files | Pass | One mode-`0700` run directory contained mode-`0600` prompt, read-back, response, and reason artifacts; a unique sentinel was absent from helper config/state. |
| K. Legacy redaction | Pass | CLI and core tests durably replaced legacy raw diagnostic bodies with categories and SHA-256 hashes. |

## Verification

- Full local suite: 63 tests passed on Python 3.14.6.
- GitHub CI for the tested candidate passed on macOS and Ubuntu.
- Official skill and plugin validators passed during release preparation.
- Protected `main` remained at the tested candidate throughout the Pro connector proof.
- The disposable proof branch remains unmerged for auditability.
- Temporary acceptance artifacts were removed after exact parent-task restoration.

This receipt contains no worker conversation ID, parent task ID, prompt body beyond public acceptance sentinels, response body beyond public acceptance sentinels, or raw diagnostic text.
